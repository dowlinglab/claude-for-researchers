% ==================== PROLOGUE: TODAY, THE LANDSCAPE ====================
% storyboard.md Prologue items 5-10 ("Today", "The landscape", "know your data
% classification", "two things to check", "everything is on GitHub", "this
% talk was built the same way"). Items 6-8's content already matched the
% storyboard when it was written (see that file's Prologue note); that part
% of this pass is a style-guide compliance fix, not a content rewrite:
% \bad{} replaces two bare \textcolor{OkVermillion} uses for "prohibited"
% (main.tex's own rule -- meaning is never carried by colour alone), and the
% two-item checklist on the last frame is trimmed but still runs over the
% ~20-25 word guideline, noted inline, for the same reason as 00_open.tex's
% cold open: a compliance checklist needs real, correct wording more than
% it needs brevity.
%
% Items 9-10 (resources overview, the meta slide) close out the Prologue,
% added/moved here 2026-09-02 -- see the meta slide's own comment below.
%
% "Today" (item 5) is new here: the scoping slide cut when the talk
% restructured, restored 2026-09-02 alongside "The claim" (00_open.tex) at
% Alex's request. Reuses \actstrip{0} like 00b_roadmap.tex's roadmap slide
% right before it -- still nothing lit, since we're not yet past the
% Prologue's own orientation beats.
%
% No \takeaway on any frame here: Alex asked (2026-09-02) to drop the
% gold-rule wrap-up bar entirely -- it was repeating each slide's title in
% different words. Every title below states the frame's point directly
% instead, so the body (table, list, act-strip) is evidence for a claim
% already made, not build-up toward one revealed at the bottom. \slidepoint
% is dropped from "The landscape, briefly" for the same reason -- the title
% now says what that big NDBlue line used to say.
%
% ND policy claims ("know your data classification", the DoD/DoW bullet)
% are sourced from notes/references.md, confirmed by direct browser read
% 2026-09-02 -- not a search snippet. Re-verify against that file's linked
% pages before the talk; policy pages like this change without notice,
% which is why that frame keeps its own "verify before the talk" hedge as
% plain body text rather than stating the policy as permanently fixed.

\begin{frame}{One year of practices, not a tool tutorial}
  \vfill
  \begin{center}\actstrip{0}\end{center}
  \vfill
  \begin{columns}[t]
    \begin{column}{0.48\textwidth}
      \textbf{This is not}
      \begin{itemize}
        \item a Claude tutorial
        \item a product comparison
        \item a claim that AI does research
      \end{itemize}
    \end{column}
    \begin{column}{0.48\textwidth}
      \textbf{This is}
      \begin{itemize}
        \item one year of practices, and their failures
        \item mostly model-independent
        \item things to change on Monday
      \end{itemize}
    \end{column}
  \end{columns}
  \vfill
\end{frame}

\begin{frame}{Don't become loyal to a model}
  \vfill
  \begin{center}
  \begin{tabular}{@{}llll@{}}
    \toprule
    & \textbf{Chat} & \textbf{Coding agent} & \textbf{Grounded on your files} \\
    \midrule
    Google    & Gemini  & ---    & NotebookLM \\
    OpenAI    & ChatGPT & Codex  & ChatGPT Projects \\
    Anthropic & Claude  & Claude Code & Claude Projects \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vfill
  \begin{center}
    \small Become good at working with models instead. Rankings
    change every few months; the workflow habits in this talk do not.
  \end{center}
  \vfill
\end{frame}

\begin{frame}{Claude is approved here for public data only}
  \vfill
  \begin{center}
  \begin{tabular}{@{}lll@{}}
    \toprule
    \textbf{Tier} & \textbf{Examples} & \textbf{AI use} \\
    \midrule
    \textcolor{OkGreen}{$\bullet$}~Public    & published, press-released & safe \\
    \textcolor{NDGold!80!black}{$\bullet$}~Internal  & routine business data & safe \emph{in approved tools} \\
    \textcolor{OkVermillion}{$\bullet$}~Sensitive & role-restricted       & safe \emph{in approved tools} \\
    \textcolor{black}{$\blacksquare$}~Restricted & SSN, health, payment  & \bad{never, in any tool} \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vfill
  \begin{center}
  \begin{tabular}{@{}lc@{}}
    \toprule
    \textbf{Tool} & \textbf{Approved through} \\
    \midrule
    Gemini, ChatGPT EDU, NotebookLM & Sensitive \\
    \textbf{Claude} & \textbf{Public only} \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vfill
  \begin{center}
    \small An enterprise license is in progress --- verify this before the talk.
  \end{center}
  \vfill
\end{frame}

\begin{frame}{Two checks, thirty seconds, before you start}
  \vfill
  \begin{itemize}\setlength\itemsep{1.2em}
    \item \textbf{DoD / DoW funding?}\\
      DoD/DoW-funded researchers are currently \bad{prohibited} from
      using any Anthropic product --- Claude.ai, the API, Claude
      Code.\\[0.3em]
      {\small Questions: \texttt{researchsecurity@nd.edu}}
    \item \textbf{Everyone else: ask your PI.}\\
      Export control, sponsor terms, and IRB conditions differ per
      project --- what's fine on one may not be on another.
  \end{itemize}
  \vfill
\end{frame}

\begin{frame}[fragile]{Everything today is on GitHub}
  \vfill
  \begin{lstlisting}
resources/
  practices/   9 files -- the doctrine
  prompts/     3 files -- ready to paste in
  scripts/     3 tools -- DOI checker, figure style,
               latexdiff-vs-last-commit
  templates/   3 files -- project file, research log,
               results manifest
  \end{lstlisting}
  \vfill
  \begin{center}
    \small Prompts, checklists, and three scripts you can use as-is today --- not a summary to remember, a repo to clone.
  \end{center}
  \vfill
\end{frame}

% Meta slide, moved here from the Epilogue 2026-09-02 at Alex's request:
% placed early so it establishes authority and foreshadows the acts
% without revealing their specific content, rather than only paying off
% at the end. Its row in the Epilogue's summary matrix completes the loop.
\begin{frame}{This talk was built the same way}
  \vfill
  \begin{enumerate}\setlength\itemsep{0.6em}
    \item Identified 10+ AI-accelerated projects from the past year
    \item Dispatched agents (Claude Opus) to examine each one's full history
    \item Distilled the findings into actionable practice guides
    \item Crafted a story arc and outline
    \item Built these slides iteratively with Claude --- including analyzing my own past talks to match my actual style
  \end{enumerate}
  \vfill
  \begin{center}
    \slidepoint{You are about to watch the thesis, not just hear it.}
  \end{center}
  \vfill
\end{frame}
