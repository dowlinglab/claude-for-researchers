% ==================== PROLOGUE: TODAY, THE LANDSCAPE ====================
% storyboard.md Prologue items 5, 6, 9, 10 ("Today", "The landscape",
% "everything is on GitHub", "this talk was built the same way"). Items
% 7-8 (data classification, the two-checks list) MOVED to the Epilogue
% 2026-09-03 at Alex's suggestion -- they read as "Trust" material, not a
% Prologue scoping beat; item 6 ("Don't become loyal to a model") stays
% here per Alex's own counter-argument: it directly cashes out this file's
% first slide ("not a product comparison"), so it belongs with the scoping
% beats, not the policy ones.
%
% "Today" (item 5) is new here: the scoping slide cut when the talk
% restructured, restored 2026-09-02 alongside "The claim" (00_open.tex) at
% Alex's request. Reuses \actstrip{0} like 00b_roadmap.tex's roadmap slide
% right before it -- still nothing lit, since we're not yet past the
% Prologue's own orientation beats.
%
% No \takeaway on any frame here: Alex asked (2026-09-02) to drop the
% gold-rule wrap-up bar entirely -- it was repeating each slide's title in
% different words. Every title below states the frame's point directly
% instead, so the body (table, list, act-strip) is evidence for a claim
% already made, not build-up toward one revealed at the bottom.
%
% "Everything today is on GitHub" REVISED 2026-09-03: was counts-only
% ("9 files -- the doctrine"), now the actual filenames, per Alex's request
% to fill the slide's space better -- and the repo link. Filenames read
% straight off the repo tree (resources/), not retyped from memory.
%
% Archetypes content ADDED 2026-09-03 at Alex's request, first as its own
% slide right before the meta slide, then FOLDED INTO this slide the same
% day once he asked to cut this slide's act-strip and use the freed space
% for the archetype preview instead -- the standalone slide it came from is
% gone (would have been a pure repeat, against style_guide.md rule 9).
%
% NUMBERED 2026-09-03 (later the same day, after the six-types-to-six-
% tasks restructure -- see storyboard.md's revision #3): "Six kinds of
% task" became "Six tasks," and each row now carries the Task 1-6 number
% that the acts themselves use, per Alex's request for consistent
% structure throughout the talk. Left column is Tasks 1-3 (Act I), right
% column is Tasks 4-6 (Act II/III) -- the two-column split already lined
% up with the acts before the renumbering, kept as is. Still doesn't say
% which example is named vs. generic (that's demo_ideas.md's own call) --
% just a preview, now with the numbers the rest of the talk will use.
%
% The act-strip itself is cut, not just relocated (Alex's own call,
% 2026-09-03): a second copy right after the roadmap slide's outline added
% nothing new, per style_guide.md rule 9.

\begin{frame}{One year of practices, not a tool tutorial}
  \vfill
  \begin{columns}[t]
    \begin{column}{0.48\textwidth}
      \textbf{This is not}
      \begin{itemize}
        \item a Claude tutorial
        \item a product comparison
        \item a claim that AI does research
      \end{itemize}
    \end{column}
    \begin{column}{0.48\textwidth}
      \textbf{This is}
      \begin{itemize}
        \item one year of practices, and their failures
        \item mostly model-independent
        \item things to change on Monday
      \end{itemize}
    \end{column}
  \end{columns}
  \vspace{0.8em}
  \begin{center}
    \textbf{Six tasks, all year}\\[0.5em]
    \small
    \begin{tabular}{@{}p{4.7cm}p{4.7cm}@{}}
      1.~Set up git for a project & 4.~Paper code $\to$ public package \\[0.5em]
      2.~Inherit a project's context & 5.~Audit a manuscript \\[0.5em]
      3.~Idea $\to$ report or proposal & 6.~Audit a course, end to end \\
    \end{tabular}
  \end{center}
  \vfill
\end{frame}

\begin{frame}{Don't become loyal to a model}
  \vfill
  \begin{center}
  \begin{tabular}{@{}llll@{}}
    \toprule
    & \textbf{Chat} & \textbf{Coding agent} & \textbf{Grounded on your files} \\
    \midrule
    Google    & Gemini  & ---    & NotebookLM \\
    OpenAI    & ChatGPT & Codex  & ChatGPT Projects \\
    Anthropic & Claude  & Claude Code & Claude Projects \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vfill
  \begin{center}
    Become good at working with models instead. Rankings
    change every few months; the workflow habits in this talk do not.
  \end{center}
  \vfill
\end{frame}

\begin{frame}[fragile]{Everything today is on GitHub}
  \vfill
  \begin{columns}[t]
    \begin{column}{0.46\textwidth}
      {\small\textbf{practices/} \scriptsize(9 -- the doctrine)}
      \begin{itemize}\scriptsize\setlength\itemsep{0.15em}
        \item grant\_proposal\_writing
        \item literature\_review
        \item manuscript\_audit
        \item private\_code\_to\_public\_package
        \item scientific\_computing\_workflow
        \item scientific\_figures\_tables
        \item technical\_writing
        \item working\_with\_ai\_agents
        \item writing\_style\_guide
      \end{itemize}
    \end{column}
    \begin{column}{0.5\textwidth}
      {\small\textbf{prompts/} \scriptsize(3 -- ready to paste in)}
      \begin{itemize}\scriptsize\setlength\itemsep{0.15em}
        \item literature\_workflow
        \item manuscript\_audit
        \item organize\_research\_repo
      \end{itemize}
      \vspace{0.5em}
      {\small\textbf{scripts/} \scriptsize(3 -- tools)}
      \begin{itemize}\scriptsize\setlength\itemsep{0.15em}
        \item doi\_checker
        \item figure\_style
        \item latexdiff\_check.sh
      \end{itemize}
      \vspace{0.5em}
      {\small\textbf{templates/} \scriptsize(3 -- fill in and go)}
      \begin{itemize}\scriptsize\setlength\itemsep{0.15em}
        \item project\_entry\_point
        \item research\_log
        \item results\_manifest
      \end{itemize}
    \end{column}
  \end{columns}
  \vfill
  \begin{center}
    \small\texttt{github.com/dowlinglab/claude-for-researchers}\\
    not a summary to remember --- a repo to clone.
  \end{center}
  \vfill
\end{frame}

% Meta slide, moved here from the Epilogue 2026-09-02 at Alex's request:
% placed early so it establishes authority and foreshadows the acts
% without revealing their specific content, rather than only paying off
% at the end. Its row in the Epilogue's summary matrix completes the loop.
% PROTOTYPE 2026-09-03, Alex's example of the size/color rethink applied
% concretely: list stays \normalsize (it already was -- no explicit
% \small here to begin with), and the tagline moves from a big centered
% \slidepoint callout to a small caption-style line beside the corner
% logo, instead of claiming its own large chunk of vertical space.
\begin{frame}{This talk was built the same way}
  \vspace{0.3cm}
  \begin{enumerate}\setlength\itemsep{0.6em}
    \item Identified 10+ AI-accelerated projects from the past year
    \item Dispatched agents (Claude Opus) to examine each one's full history
    \item Distilled the findings into actionable practice guides
    \item Crafted a story arc and outline
    \item Built these slides iteratively with Claude --- including analyzing my own past talks to match my actual style
  \end{enumerate}
  \vfill
  \begin{tikzpicture}[remember picture, overlay]
    \node[anchor=south west, xshift=3.4cm, yshift=2mm, align=left, text width=8.6cm]
      at (current page.south west)
      {\small\itshape You are about to \textbf{watch} the thesis, not just \textbf{hear} it.};
  \end{tikzpicture}
\end{frame}
