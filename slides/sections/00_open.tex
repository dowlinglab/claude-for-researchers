% ======================= PROLOGUE: THE CLAIM, THE TEASER =======================
% storyboard.md Prologue items 2 ("The claim") and 3 ("The teaser"). Item 1
% (title) is in main.tex; item 4 ("Borrowed structure") is already built in
% 00b_roadmap.tex. The graduating-student hook that used to sit between
% these two predates the three-act restructure and is gone from here; it
% survives as Act I's generic inherited-project case, not as the opener.
%
% "The claim" was cut when the talk restructured, on the reasoning that
% storyboard.md's own Prologue list didn't carry it forward; restored
% 2026-09-02 at Alex's request (he liked the slide and would rather cut
% elsewhere). Wording matches the thesis stated in CLAUDE.md and the
% submitted abstract -- this is the one slide that says it as a claim.
%
% "The teaser": full story deliberately withheld -- the PyPI page, tag list,
% and commit graph are held for Act II slide 1's full return (image_plan.md),
% so the visual reveal isn't spent twice. The closing question is left open
% on purpose; Epilogue slide 1 returns to it verbatim.
%
% The teaser runs a little over style_guide.md's ~20-25 word narrative
% guideline (about 45 words): this is the talk's cold open and has to carry
% real, specific, credibility-establishing facts (dates, versions, which
% tool) on the way to the question.

\begin{frame}{The claim}
  \vfill
  \begin{center}
    The most powerful uses of generative AI in research go beyond\\
    asking a chatbot questions or polishing prose.
  \end{center}
  \vspace{1.2em}
  \begin{center}
    \slidepoint{They come from putting AI inside a workflow that is\\
    deliberate, reproducible, and auditable.}
  \end{center}
  \vfill
\end{frame}

\begin{frame}{A weekend project}
  \vfill
  \begin{center}
    \slidepoint{One weekend, an annual scheduling headache,\\and a package on PyPI.}
  \end{center}
  \vspace{1.2em}
  \begin{center}
    \small \keyterm{grad-visit-scheduler}: v0.1.2 $\to$ v0.3.1 in a week ---\\
    built with Codex, not Claude.
  \end{center}
  \vfill
  \begin{center}
    \small\itshape
    ``5$\times$ productivity, but I'd already touched every\\
    stage of this by hand.''
  \end{center}
  \vspace{0.8em}
  \begin{center}
    \slidepoint{How do we train the next generation to get both?}
  \end{center}
  \vfill
\end{frame}
