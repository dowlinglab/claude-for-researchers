% ======================= PROLOGUE: THE HOOK, THE CLAIM =======================
% storyboard.md Prologue items 2-3 ("The teaser", "The claim"). Item 4
% ("Borrowed structure") is 00b_roadmap.tex; item 1 (title) is in main.tex.
%
% Every date/fact below is git-verified against both real repos (private:
% ndcbe/grad-visit-scheduling; public: dowlinglab/grad-visit-scheduler), not
% recollection: the Codex conversion ran Mon Feb 9 -- Tue Feb 10, 2026
% (weekdays, not a weekend); DGA is a generic title (Director of Graduate
% Admissions), not attributed to a specific person. The 2023 box is
% co-built (36 Alex + 37 Jeff commits); 2024/2025 are solo (100% Alex).
%
% Frame 1's schedule image is a REAL run of the real, public package
% (v0.5.0, installed from PyPI) -- real ND CBE faculty (public roster),
% fictional Winnie-the-Pooh visitors (public domain) standing in for real
% prospective students. Generation script is throwaway, not checked into
% this repo; regenerate via grad-visitor-scheduler's
% SolutionResult.plot_visitor_schedule() (examples/README.md in the public
% repo has the pattern). The demo config MUST set `movement: {policy:
% travel_time, min_buffer_minutes: 10}` -- `policy: none` silently
% disables travel-time enforcement between buildings and produces
% impossible back-to-back transitions. Image height is capped at 6.0cm:
% past that, the image's own white canvas (not just its content) collides
% with the corner logo.
%
%
\begin{frame}{Creating faculty-visitor for graduate student recruitment}
  \vfill
  \begin{columns}[c]
    \begin{column}{0.52\textwidth}
      \centering
      % TODO: Please make this figure larger. Also, please make the faculty name boxes both longer and taller. The faculty names overrun the boxes. This will be a nice enhancement you can contribute to the scheduling package. This would be a great task for a worker.
      \includegraphics[height=6.0cm]{figures/grad_visit_schedule_6}
    \end{column}
    \begin{column}{0.44\textwidth}
      \centering
      % TODO: Please add white space above the diagram such that there is
      % equal white space between the top and bottom
      \begin{tikzpicture}[font=\scriptsize, node distance=0.5cm]
        \node[draw=OkVermillion, rounded corners=2pt, minimum width=6.2cm, minimum height=1cm, text width=5.8cm, align=center]
          (a) {\textbf{Before 2023}\\DGA spent \textasciitilde10 hours (re)making the schedule by hand};
        \node[draw=NDBlue, rounded corners=2pt, minimum width=6.2cm, minimum height=1cm, text width=5.8cm, align=center, below=of a]
          (b) {\textbf{2023}\\Jeff Kantor and I built a Jupyter notebook that used discrete optimization to create the schedule.};
        \node[draw=NDBlue, rounded corners=2pt, minimum width=6.2cm, minimum height=1cm, text width=5.8cm, align=center, below=of b]
          (c) {\textbf{2024--2025}\\I maintained the notebook, patching it by hand, inputting data, and creating schedules each year.};
        \node[draw=NDGold, rounded corners=2pt, minimum width=6.2cm, minimum height=1cm, text width=5.8cm, align=center, below=of c, fill=CodeBg]
          (d) {\textbf{Feb 2026}\\Using Codex, I converted the schedule optimization code into a tested, public Python package.};
        \draw[-{Stealth[length=6pt]}, thick] (a) -- (b);
        \draw[-{Stealth[length=6pt]}, thick] (b) -- (c);
        \draw[-{Stealth[length=6pt]}, thick] (c) -- (d);
      \end{tikzpicture}
    \end{column}
  \end{columns}
  \vspace{1.8cm plus 1fill}
  % TODO: Add the GitHub package url in the slide footer (part of our temlate)
\end{frame}

% Table rows are each verified against the public repo's own CHANGELOG.md;
% docs-only tags are left out. The two "---" rows before the first real
% tag are real, timestamped private-repo commits (Mon Feb 9, 2026: "Draft
% 1 ready and sent to Carla for feedback" 14:43; "Started to restructure
% as a package" 22:03). The Read the Docs screenshot independently
% confirms "Created by Alex Dowling and Jeff Kantor."
%
% "10 hours" in the title is Alex's own account of active hands-on-
% keyboard time, not calendar-elapsed time -- git timestamps alone span
% ~18.5 elapsed hours (refactor start, Mon 10:03pm, to first PyPI release,
% Tue 4:39pm) crossing an overnight gap, which can't confirm or rule out
% actual working time. Not independently git-verifiable either way.
\begin{frame}[fragile]{Jupyter Notebook $\to$ public Python package in ~10 hours}
  \begin{columns}[T]
    \begin{column}{0.46\textwidth}
      \begin{minipage}[t]{\linewidth}
      \centering
      \footnotesize
      \begin{tabular}{@{}lll@{}}
        \toprule
        \textbf{Ver.} & \textbf{Date} & \textbf{What changed} \\
        \midrule
        \texttt{---} & Feb 9 & Draft 1 of 2026 schedule \\
        \texttt{---} & Feb 9 & notebook $\to$ tested pkg \\
        \texttt{v0.1.1} & Feb 10 & Initial public release \\
        \texttt{v0.2.0} & Feb 13 & Top-N schedules \\
        \texttt{v0.3.0} & Feb 13 & Multi-building, travel time \\
        \texttt{v0.3.1} & Feb 17 & Full tests, docs complete \\
        \bottomrule
      \end{tabular}
      \end{minipage}
    \end{column}
    \begin{column}{0.5\textwidth}
      \begin{minipage}[t]{\linewidth}
      \centering
      % TODO: This image does not need a caption
      \includegraphics[width=\linewidth]{figures/grad_scheduler_screenshot_crop}\\[0.3em]
      {\scriptsize The package's own Read the Docs page, live today.}
      \end{minipage}
    \end{column}
  \end{columns}
  \vspace{0.4em}
  \begin{center}
    On sabbatical this past February, I nearly said no. Then, I decided to use this as an excuse to really try Codex.
  \end{center}
  \vspace{0.3em}
  \begin{center}
    After this small project, I got everyone in my group an \textbf{ND OpenAI seat}. We regularly discuss \textbf{AI best wins, traps, and practices} in our weekly meeting.
  \end{center}
  \vspace{1.8cm plus 1fill}
  % TODO: Pleaes add the URL to the ReadTheDocs documentation in the footer option defined in the custom beamer template
\end{frame}

\begin{frame}{Takeaway message for today}
  \vfill
  \begin{center}
  	% TODO: Update this to the slidepoint. I want it to be the larger
  	% font similar to the the other slides
    The most powerful uses of generative AI in research go beyond\\
    asking a chatbot questions or polishing prose.
  \end{center}
  \vspace{1.2em}
  \begin{center}
    \slidepoint{They come from putting AI inside a workflow that is\\
    \textbf{deliberate, reproducible, and auditable}.}
  \end{center}
  \vfill
\end{frame}
