% ======================= PROLOGUE: THE HOOK, THE CLAIM =======================
% storyboard.md Prologue items 2-3 ("The teaser", "The claim") -- REVAMPED
% 2026-09-03 at Alex's request: the old single-slide teaser undersold the
% hook. Now three frames: two tell the grad-visit-scheduler story with real
% visuals (the "before" artifact, then the Codex conversion), and "The
% claim" -- previously the first frame after the title -- now lands after
% the story as the takeaway, not before it. Item 4 ("Borrowed structure")
% is 00b_roadmap.tex; item 1 (title) is in main.tex.
%
% Facts (DGA, Carla, the ~1.5hr/~8.5hr split, 100% test coverage) are
% Alex's own account, given directly 2026-09-03. Two things deliberately
% NOT stated because they'd be invented, not sourced:
%   - The year the Jeff-Kantor notebook was built ("In XYZ" in Alex's own
%     notes) -- said as "years later" instead. Fill in the real year here
%     if you want it on the slide.
%   - DGA is generic (Director of Graduate Admissions), not attributed to
%     a specific person -- Alex confirmed the title but not whose role it
%     was historically, so the slide doesn't guess.
%
% The frame-1 schedule grid is a SYNTHETIC mockup, not a real screenshot --
% different rule than every other "real artifact" TODO in this deck. A real
% generated schedule contains visiting students' and faculty's real names
% and times: that's exactly the kind of personal data this deck's own
% data-classification slide (now in the Epilogue) says not to publish.
% Illustrative-only, labelled as such, by design -- not a placeholder
% waiting on a screenshot pass.
%
% TODO(image_plan.md Prologue #3): frame 2's version-timeline TikZ is
% real (v0.1.2->v0.3.1, Feb 11-17) -- upgrade the surrounding text to real
% screenshots (PyPI page, GitHub tags, commit graph) once the browser-
% capture pass runs; see image_plan.md for exact URLs. The timeline itself
% moved here from Act II slide 1, which no longer repeats it (see that
% file's own header comment).
%
% The closing question ("How do we train...") is unchanged text, still the
% exact line Epilogue slide 1 returns to verbatim -- do not reword either
% copy without updating the other.

\begin{frame}[fragile]{Before Codex: ten hours, a notebook, then patches}
  \vfill
  \begin{columns}[c]
    \begin{column}{0.44\textwidth}
      \centering
      {\scriptsize\itshape A visit-day schedule --- illustrative, not real data}\\[0.4em]
      \begin{tabular}{@{}lccc@{}}
        \toprule
        & \textbf{9:00} & \textbf{10:00} & \textbf{11:00} \\
        \midrule
        Visitor A & Prof.~1 & Prof.~4 & Prof.~7 \\
        Visitor B & Prof.~2 & Prof.~5 & Prof.~8 \\
        Visitor C & Prof.~3 & Prof.~6 & Prof.~9 \\
        \bottomrule
      \end{tabular}
    \end{column}
    \begin{column}{0.52\textwidth}
      \small
      Every visit weekend needs one of these --- who meets whom, when. Years ago, built by hand: about 10 hours for the DGA, redone from scratch for every faculty and visitor change.\\[0.7em]
      Years later, \keyterm{Jeff Kantor} and I wrote a Python notebook to formulate and solve it as an optimization problem --- then kept bolting on features, like auto-generating each visitor's schedule as a Word document.
    \end{column}
  \end{columns}
  \vfill
\end{frame}

\begin{frame}[fragile]{One weekend with Codex, and I kept going}
  \vfill
  \begin{center}
  \begin{tikzpicture}[font=\small]
    \foreach \x/\lbl in {0/v0.1.2, 2.2/v0.2.0, 4.4/v0.2.5, 6.6/v0.3.1} {
      \fill[NDGold] (\x,0) circle (2.5pt);
      \node[below] at (\x,-0.15) {\lbl};
    }
    \draw[thick, NDBlue] (0,0) -- (6.6,0);
    \node[above] at (0,0.3) {\scriptsize Feb 11};
    \node[above] at (6.6,0.3) {\scriptsize Feb 17};
  \end{tikzpicture}
  \end{center}
  \vspace{0.3em}
  \begin{center}
    \small On sabbatical last year, I nearly turned down the ask to build the schedule again --- then decided to use it as an excuse to really try Codex.
  \end{center}
  \vspace{0.3em}
  \begin{center}
    \small About 1.5 hours: Codex reformatted \keyterm{Carla}'s spreadsheet and regenerated the schedule. I was stunned, so I kept going --- 8.5 more hours turned the notebook into a tested, documented, pip-installable package (\texttt{grad-visitor-scheduler}) with 100\% test coverage, Codex writing the tests and hunting the edge cases.
  \end{center}
  \vspace{0.5em}
  \begin{center}
    {\small\itshape Live, right now: adding one feature, start to finish, in about 20 minutes.}
  \end{center}
  \vspace{0.6em}
  \begin{center}
    \slidepoint{How do we train the next generation to get both?}
  \end{center}
  \vfill
\end{frame}

\begin{frame}{The claim}
  \vfill
  \begin{center}
    The most powerful uses of generative AI in research go beyond\\
    asking a chatbot questions or polishing prose.
  \end{center}
  \vspace{1.2em}
  \begin{center}
    \slidepoint{They come from putting AI inside a workflow that is\\
    deliberate, reproducible, and auditable.}
  \end{center}
  \vfill
\end{frame}
