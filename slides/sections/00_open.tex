% ============================ PROLOGUE: THE TEASER ============================
% storyboard.md Prologue item 2, "The teaser". Item 1 (title) is in main.tex;
% item 3 ("Borrowed structure") is already built in 00b_roadmap.tex. This is
% the only frame this file owns now -- the old three-frame version (a "the
% claim" thesis slide and a "graduating student" hook) predates the three-act
% restructure and is gone; the graduating-student story survives as Act I's
% generic inherited-project case, not as the opening hook.
%
% Full story deliberately withheld: this is a teaser, not the payoff -- the
% PyPI page, tag list, and commit graph are held for Act II slide 1's full
% return (image_plan.md), so the visual reveal isn't spent twice. The closing
% question is left open on purpose; Epilogue slide 1 returns to it verbatim.
%
% Runs a little over style_guide.md's ~20-25 word narrative guideline (about
% 45 words here): this is the talk's cold open and has to carry real,
% specific, credibility-establishing facts (dates, versions, which tool) on
% the way to the question. Kept to one frame regardless, since storyboard.md
% costs the Prologue at exactly 7 slides.

\begin{frame}{A weekend project}
  \vfill
  \begin{center}
    \slidepoint{One weekend, an annual scheduling headache,\\and a package on PyPI.}
  \end{center}
  \vspace{1.2em}
  \begin{center}
    \small \keyterm{grad-visit-scheduler}: v0.1.2 $\to$ v0.3.1 in a week ---\\
    built with Codex, not Claude.
  \end{center}
  \vfill
  \begin{center}
    \small\itshape\color{Muted}
    ``5$\times$ productivity, but I'd already touched every\\
    stage of this by hand.''
  \end{center}
  \vspace{0.8em}
  \begin{center}
    \slidepoint{How do we train the next generation to get both?}
  \end{center}
  \vfill
\end{frame}
