% ============================ EPILOGUE: TRUST ============================
% storyboard.md Epilogue. Job: land the responsible-use synthesis, close
% on curiosity rather than a caveat.
%
% CUT 2026-09-03, Alex's call ("I am leaning towards (b), just cut"): the
% "How do we train the next generation to get both?" return-to-the-hook
% slide. Its "we don't have a clean answer" rhetoric was written for a
% sparse, single-slide hook that stayed open until here -- the hook isn't
% sparse anymore (the Prologue now resolves it in real depth, and Act II
% resolves its impact explicitly), so returning to "no clean answer" no
% longer fit. The Prologue's hook still closes on the same question
% (00_open.tex) -- that line is untouched, it just no longer has an
% explicit "we're back to this" answer slide here. It stands as an open
% hook the rest of the talk answers implicitly, not one this act revisits.
%
% The "this talk was built the same way" meta-slide moved to the Prologue
% (01_ecosystem.tex, after "What you get today") 2026-09-02, Alex's
% revised call: placed early it establishes authority and foreshadows the
% acts without revealing their specific content, rather than only paying
% off at the end. Its summary-matrix row below is what completes the loop
% here instead.
%
% MOVED HERE 2026-09-03 at Alex's suggestion: the two IT-policy slides
% ("Claude is approved here for public data only", "Two checks, thirty
% seconds, before you start") used to sit in the Prologue
% (01_ecosystem.tex); they fit "Trust" better than the Prologue's
% scoping beats, so they moved. Placed after the hook's question is
% answered (not before it): they're the practical form of "trust," right
% before the summary matrix, rather than a compliance interruption before
% the talk has even started.

\stagedivider{5}{Epilogue: Trust}{What do you trust, and why?}

% Spacing tightened 2026-09-03 (vfill -> fixed vspace between the two
% tables) to clear the bigger, corner-tighter logo -- two tables plus a
% closing line left little room to spare even before that change.
\begin{frame}{Claude is approved here for public data only}
  \vfill
  \renewcommand{\arraystretch}{0.9}
  \begin{center}
  \begin{tabular}{@{}lll@{}}
    \toprule
    \textbf{Tier} & \textbf{Examples} & \textbf{AI use} \\
    \midrule
    \textcolor{OkGreen}{$\bullet$}~Public    & published, press-released & safe \\
    \textcolor{NDGold!80!black}{$\bullet$}~Internal  & routine business data & safe \emph{in approved tools} \\
    \textcolor{OkVermillion}{$\bullet$}~Sensitive & role-restricted       & safe \emph{in approved tools} \\
    \textcolor{black}{$\blacksquare$}~Restricted & SSN, health, payment  & \bad{never, in any tool} \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vspace{0.5em}
  \begin{center}
  \begin{tabular}{@{}lc@{}}
    \toprule
    \textbf{Tool} & \textbf{Approved through} \\
    \midrule
    Gemini, ChatGPT EDU, NotebookLM & Sensitive \\
    \textbf{Claude} & \textbf{Public only} \\
    \bottomrule
  \end{tabular}
  \end{center}
  \renewcommand{\arraystretch}{1}
  \vspace{0.5em}
  \begin{center}
    \small An enterprise license is in progress --- verify this before the talk.
  \end{center}
  \vspace{1.8cm plus 1fill}
\end{frame}

\begin{frame}{Two checks, thirty seconds, before you start}
  \vfill
  \begin{itemize}\setlength\itemsep{1.2em}
    \item \textbf{DoD / DoW funding?}\\
      DoD/DoW-funded researchers are currently \bad{prohibited} from
      using any Anthropic product --- Claude.ai, the API, Claude
      Code.\\[0.3em]
      {\small Questions: \texttt{researchsecurity@nd.edu}}
    \item \textbf{Everyone else: ask your PI.}\\
      Export control, sponsor terms, and IRB conditions differ per
      project --- what's fine on one may not be on another.
  \end{itemize}
  \vfill
\end{frame}

% Relabeled 2026-09-03 with the same Task 1-6 numbers used throughout the
% talk (Prologue preview, Act I/II/III titles) -- was prose descriptions
% in six-types order, now the actual numbers in order, so this reads as a
% literal recap rather than a re-derivation. Rows reordered to match.
% Task 3 spans Act I (idea -> report/proposal) and Act II (the proposal's
% compliance-review payoff, "Task 3, continued"). Task 4's Prologue check
% reflects the grad-visit-scheduler hook -- deliberately unnumbered
% (Alex's call, it's "very similar to Task 4"), but the same kind of task
% really does recur there, so the checkmark stays honest to that.
\begin{frame}{A handful of projects, recurring}
  \vfill
  \begin{center}
  \small
  \begin{tabular}{@{}lccccc@{}}
    \toprule
    & \textbf{Prologue} & \textbf{Act I} & \textbf{Act II} & \textbf{Act III} \\
    \midrule
    Task 1 --- set up git & & \good{} & & \\
    Task 2 --- inherit a project's context & & \good{} & & \\
    Task 3 --- idea $\to$ report or proposal & & \good{} & \good{} & \\
    Task 4 --- paper code $\to$ package & \good{} & & \good{} & \\
    Task 5 --- audit a manuscript & & & & \good{} \\
    Task 6 --- audit a course, end to end & & & & \good{} \\
    Creating this talk & \good{} & & & \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vfill
  \begin{center}
    \small Six numbered tasks, plus this talk itself --- a handful of real projects, recurring across every act, shown not asserted.
  \end{center}
  \vfill
\end{frame}

\begin{frame}{You own every claim, whatever helped you make it}
  \vfill
  \begin{itemize}\setlength\itemsep{0.9em}
    \item AI drafts, checks, and challenges. It does not decide what's true.
    \item Disclose AI use the way you'd disclose a collaborator's contribution.
    \item Every claim in this talk was independently verified before it went on a slide --- date, quote, commit hash.
    \item The same standard applies to your own work, whatever helped you produce it.
  \end{itemize}
  \vfill
\end{frame}

% REVISED 2026-09-03 at Alex's request: much less text, the figure much
% larger on the left, the right column now a real project list instead
% of one paragraph about the first project only. Verified against the
% real, public adowling2/3d-printing repo (65 commits, Alex alone, Feb
% 15 - Sep 2, 2026) -- most projects share a first-commit date, Feb 15,
% 2026, the day the repo (and its workflow/ tooling: init_project.sh,
% render_views.sh, a STYLE.md, a Codex-iteration-loop convention) was
% first set up; tablet-car-center was added Aug 8, six months later,
% same workflow still in use. keychains has by far the most activity
% (30 commits) -- the most-iterated project, a real number, not an
% estimate.
\begin{frame}{Curiosity is a feature, not a consolation prize}
  \vfill
  \begin{columns}[c]
    \begin{column}{0.46\textwidth}
      \centering
      \includegraphics[height=4.2cm]{figures/boot_dryer_octopus.jpeg}\\[0.15em]
      {\scriptsize Hesburgh Libraries' 3D printing (credit: Adam Heet).}
    \end{column}
    \begin{column}{0.48\textwidth}
      \small
      Same discipline, git-tracked: a script to start a project, a script to render it, one shared style guide.
      \begin{itemize}\scriptsize\setlength\itemsep{0.25em}
        \item boot dryer (the octopus)
        \item bathroom \& drawer organizers
        \item telescope shelf \& caps, camera \& binocular mounts
        \item keychains --- 30 commits and counting
        \item tablet car mount --- six months later, same workflow
      \end{itemize}
    \end{column}
  \end{columns}
  \vspace{0.5em}
  \begin{center}
    \slidepoint{What would 3D printing \textbf{teach} in a CBE course ---\\ turbulent-mixing baffles, a vehicle for thermo, transport, controls?}
  \end{center}
  \vspace{1.8cm plus 1fill}
\end{frame}

% NEW 2026-09-03 at Alex's request: a second hobby slide, generalized as
% "create a textbook customized for you." Verified against the real,
% public adowling2/radio-extra-book repo (87 commits, Alex alone, Jul 22
% - Sep 2, 2026): "Circuit Theory for the Amateur Extra Exam" -- 26
% chapters in 5 real \part{} sections (main.tex), 226 pages. The 5 steps
% are drawn from the real commit history and PROJECT-STATUS.md, not
% invented: source drafted chapter by chapter, then a "v2 reorg" into
% the current part structure, a Python figure pipeline verified to
% regenerate byte-identically, coverage checked against real ARRL exam
% question-pool extracts (greppable, not hand-transcribed), and
% structured revision rounds tracked in a ledger -- "58 done, 0 open" is
% the real, current count from ARRL-GAP-PROPOSAL.md, not an estimate.
% Two of those 58 were the book catching its own errors (Ch 22 had the
% wrong reason for coil Q falling; another mistold push-pull/IMD) --- the
% closing line's "two were the book's own errors" is real, not a
% rhetorical flourish. Deliberately not numbered "Task 7": this is hobby
% material, same register as the slide before it, kept outside the
% professional six-task taxonomy by design (demo_ideas.md's own framing)
% -- flag if you actually want it folded into the numbered curriculum,
% since that would ripple into the Prologue preview and Epilogue matrix.
\begin{frame}{Create a textbook, customized for you}
  \vfill
  \begin{columns}[t]
    \begin{column}{0.44\textwidth}
      \small
      \textbf{Circuit Theory for the Amateur Extra Exam}\\
      {\scriptsize A control-theory perspective --- Alex Dowling, KE9FZD}\\[0.6em]
      \begin{enumerate}\scriptsize\setlength\itemsep{0.25em}
        \item Exam-Ready Reference
        \item Mathematical \& Control-Theory Foundations
        \item Circuit Models
        \item Applying the Circuit Models
        \item Practice \& Study
      \end{enumerate}
    \end{column}
    \begin{column}{0.52\textwidth}
      \small
      \begin{enumerate}\setlength\itemsep{0.35em}
        \item Draft the source, chapter by chapter
        \item Reorganize into a coherent part structure
        \item Build a reproducible figure pipeline
        \item Verify coverage against the real exam question pools
        \item Revise in structured rounds --- audit, fix, re-verify
      \end{enumerate}
    \end{column}
  \end{columns}
  \vfill
  \begin{center}
    \slidepoint{58 revisions later, \textbf{0 open} ---\\two were the book's \textbf{own errors}, caught by its own audit.}
  \end{center}
  \vspace{1.8cm plus 1fill}
\end{frame}

\begin{frame}{One thing to change Monday morning}
  \vfill
  \begin{center}
    \large Put your project's goals, conventions, and authoritative sources\\ in a file an AI can read --- don't re-explain them in every chat.
  \end{center}
  \vfill
  \begin{center}
    \small Everything in this talk --- prompts, checklists, scripts, this deck's own source --- is on GitHub.
  \end{center}
  \vfill
\end{frame}
