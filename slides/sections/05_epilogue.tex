% ============================ EPILOGUE: TRUST ============================
% storyboard.md Epilogue. Job: land the responsible-use synthesis, close
% on curiosity rather than a caveat. The Prologue's hook closes on an open
% question (00_open.tex) that this act answers implicitly rather than
% revisiting explicitly. The meta-slide ("this talk was built the same
% way") lives in the Prologue instead (01_ecosystem.tex); its row in the
% summary matrix below completes the loop.

\stagedivider{5}{Epilogue: Trust}{What do you trust, and why?}

% Spacing between the two tables uses a fixed vspace, not \vfill -- two
% tables plus a closing line leave little room to spare beside the corner
% logo.
\begin{frame}{Currently, Claude is approved for public data only at ND}
  \vfill
  \renewcommand{\arraystretch}{0.9}
  \begin{center}
  \begin{tabular}{@{}lll@{}}
    \toprule
    \textbf{Tier} & \textbf{Examples} & \textbf{AI use} \\
    \midrule
    \textcolor{OkGreen}{$\bullet$}~Public    & published, press-released & safe \\
    \textcolor{NDGold!80!black}{$\bullet$}~Internal  & routine business data & safe \emph{in approved tools} \\
    \textcolor{OkVermillion}{$\bullet$}~Sensitive & role-restricted       & safe \emph{in approved tools} \\
    \textcolor{black}{$\blacksquare$}~Restricted & SSN, health, payment  & \bad{never, in any tool} \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vspace{0.5em}
  \begin{center}
  \begin{tabular}{@{}lc@{}}
    \toprule
    \textbf{Tool} & \textbf{Approved through} \\
    \midrule
    Gemini, ChatGPT EDU, NotebookLM & Sensitive \\
    \textbf{Claude} & \textbf{Public only} \\
    \bottomrule
  \end{tabular}
  \end{center}
  \renewcommand{\arraystretch}{1}
  \vspace{0.5em}
  \begin{center}
    \small Claude Team accounts (free for academic labs) do not train on your data by default, but have a risk/reward conversation with your PI.
  \end{center}
  \vspace{1.8cm plus 1fill}
\end{frame}

\begin{frame}{Two important notes}
  \vfill
  \begin{itemize}\setlength\itemsep{1.2em}
    \item \textbf{DoD / DoW funding?}\\
      Certain funding agencies currently \bad{prohibit}
      using any Anthropic product --- Claude.ai, the API, Claude
      Code.\\[0.3em]
      {\small Questions: \texttt{researchsecurity@nd.edu} (CC your PI)}
    \item \textbf{Everyone else: ask your PI.}\\
      Export control, sponsor terms, intellectual property protection, and IRB conditions differ per
      project
  \end{itemize}
  \vfill
\end{frame}

% Rows use the same Task 1-7 numbers used throughout the talk (Prologue
% preview, Act I/II/III titles). Task 3 spans Act I (idea -> report/
% proposal) and Act II (the proposal's compliance-review payoff, "Task 3,
% continued"). Task 4's Prologue check reflects the grad-visit-scheduler
% hook -- deliberately unnumbered there, but the same kind of task
% recurs, so the checkmark stays honest to that. Tasks 6 and 7 live
% entirely in Act III, so both check only that column, same convention as
% Task 5.
\begin{frame}{A handful of projects, recurring}
  \vfill
  \begin{center}
  \small
  \begin{tabular}{@{}lccccc@{}}
    \toprule
    & \textbf{Prologue} & \textbf{Act I} & \textbf{Act II} & \textbf{Act III} \\
    \midrule
    Task 1 --- set up git & & \good{} & & \\
    Task 2 --- inherit a project's context & & \good{} & & \\
    Task 3 --- idea $\to$ report or proposal & & \good{} & \good{} & \\
    Task 4 --- paper code $\to$ package & \good{} & & \good{} & \\
    Task 5 --- audit a manuscript & & & & \good{} \\
    Task 6 --- inherit a project $\to$ manuscript & & & & \good{} \\
    Task 7 --- audit a course, end to end & & & & \good{} \\
    Creating this talk & \good{} & & & \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vfill
  \begin{center}
    Seven numbered tasks, plus this talk itself --- a handful of real projects, recurring across every act, shown not asserted.
  \end{center}
  \vfill
\end{frame}

\begin{frame}{You own every claim, whatever helped you make it}
  \vfill
  \begin{itemize}\setlength\itemsep{0.9em}
    \item AI drafts, checks, and challenges. It does not decide what's true.
    \item Disclose AI use the way you'd disclose a collaborator's contribution --- journals increasingly expect it named as a research method, not left in a footnote.
    \item Every claim in this talk was independently verified before it went on a slide --- date, quote, commit hash.
    \item The same standard applies to your own work, whatever helped you produce it.
  \end{itemize}
  \vfill
\end{frame}

% Verified against the real, public adowling2/3d-printing repo (65
% commits, Alex alone, Feb 15 - Sep 2, 2026): most projects share a
% first-commit date, Feb 15, 2026, the day the repo's workflow/ tooling
% (init_project.sh, render_views.sh, a STYLE.md, a Codex-iteration-loop
% convention) was first set up; keychains has the most activity (30
% commits). Design happens in OpenSCAD, iterated with Codex against
% workflow/STYLE.md; render_views.sh's default renderer crashes in
% headless mode (documented NSOpenGLContext errors), with
% render_stl_views_blender.sh as the real, documented fallback -- the
% same tool-for-the-task principle Task 7 tells with Codex/GPT-5.6, at
% hobby scale. Image height is capped at 4.6cm, the most the 0.42-width
% column can take without exceeding its own width (1:1 aspect ratio).
\begin{frame}{Task 8 (hobbies) --- curiosity is a feature, not a consolation prize}
  \vfill
  \begin{columns}[c]
    \begin{column}{0.42\textwidth}
      \centering
      \includegraphics[height=4.6cm]{figures/kitchen_organizer_telescope_shelf.jpeg}
    \end{column}
    \begin{column}{0.52\textwidth}
      \textbf{\small My workflow}
      \begin{enumerate}\small\setlength\itemsep{0.1em}
        \item \texttt{init\_project.sh} to start
        \item Design in OpenSCAD, iterating with Codex against one shared \texttt{STYLE.md}
        \item \texttt{render\_views.sh} for the views --- falls back to Blender when OpenSCAD's own offscreen renderer crashes
      \end{enumerate}
      \vspace{0.4em}
      \textbf{\small Projects}
      \begin{itemize}\small\setlength\itemsep{0.1em}
        \item boot dryer (the octopus)
        \item bathroom \& drawer organizers
        \item telescope shelf \& caps, camera \& binocular mounts
        \item personalized CBE keychains (recruitment)
        \item tablet car mount (kids)
      \end{itemize}
    \end{column}
  \end{columns}
  \vspace{1.8cm plus 1fill}
\end{frame}

% Verified against the real, public adowling2/radio-extra-book repo (87
% commits, Alex alone, Jul 22 - Sep 2, 2026): "Circuit Theory for the
% Amateur Extra Exam," 26 chapters, 226 pages. The 5 steps are drawn from
% the real commit history and PROJECT-STATUS.md: source drafted chapter
% by chapter, a "v2 reorg" into the current part structure, a figure
% pipeline verified to regenerate byte-identically, coverage checked
% against real ARRL exam question-pool extracts, structured revision
% rounds. Two of the tracked revisions were the book catching its own
% errors (a wrong reason given for coil Q falling; a mistold push-pull/
% IMD explanation) -- real, not a rhetorical flourish. Labeled "Task 8
% (hobbies)" -- Alex's own call: a real number, but deliberately outside
% the professional Task 1-7 taxonomy, so it does not appear in the
% Prologue's task preview or the Epilogue's summary matrix.
%
% Figure is real, from the book itself:
% chapters/15_filters_and_matching.tex's \label{fig:filter-families} --
% order-5 low-pass responses (Butterworth/Chebyshev/elliptic/Bessel),
% normalized to the same cutoff, trading flatness against transition
% sharpness against passband ripple.
\begin{frame}{Task 8 (hobbies) --- create a textbook, customized for you}
  \ndlogonote{\scriptsize\href{https://github.com/adowling2/radio-extra-book}{\texttt{github.com/adowling2/radio-extra-book}}}
  \vfill
  \begin{center}
    I want to \textbf{learn} AC circuit theory rather than memorize answers for the top-level \textbf{amateur radio license exam}. With Claude, I created a \textbf{personalized textbook}.
  \end{center}
  \vspace{0.35em}
  \begin{columns}[c]
    \begin{column}{0.44\textwidth}
      \textbf{\small My approach}
      \begin{enumerate}\small\setlength\itemsep{0.1em}
        \item Brainstorm with ChatGPT the connections between AC circuit theory and my background
        \item Draft the source, chapter by chapter
        \item Reorganize into a coherent part structure
        \item Build a reproducible figure pipeline
        \item Verify coverage against real exam question pools
        \item Revise in structured rounds
      \end{enumerate}
    \end{column}
    \begin{column}{0.48\textwidth}
      \centering
      \includegraphics[height=2.9cm]{figures/filter_families}\\[0.05em]
      {\scriptsize Order-5 low-pass filter families --- flatness, sharpness, and ripple, traded three ways.}
    \end{column}
  \end{columns}
  \vspace{1.8cm plus 1fill}
\end{frame}

\begin{frame}{One thing to change tomorrow morning}
  \vfill
  \begin{center}
    \large Put your project's goals, conventions, and authoritative sources\\ in a file an AI can read --- don't re-explain them in every chat.
  \end{center}
  \vfill
  \begin{center}
    Everything in this talk --- prompts, checklists, scripts, this deck's own source --- is on GitHub.
  \end{center}
  \vfill
\end{frame}
