% ========================= ACT III: CHALLENGE =========================
% storyboard.md Act III, revised 2026-09-03 around Task 5 and Task 6. Job:
% AI tells you that you're wrong, and you have to decide whether to
% believe it. Task 5 is manuscript audits (Type 2, generic per the naming
% convention -- unpublished work). Task 6, the capstone, names
% "Optimization for Decision Science" (Alex's own, already-public course)
% and watches one project across Understand -> Build -> Challenge.
%
% REVISED 2026-09-03, Alex reading the outline directly: "Does Task 5 need
% that many slides? We need a slide to set up Task 5." Two changes, net
% slide count unchanged:
%   1. A new setup slide (below) states the task before any technique
%      does -- every other task (1-4, in Act I/II) opens with an explicit
%      "Task N --" framing; Task 5 didn't, it jumped straight into a
%      method point.
%   2. "Trace every number back to the file" and "'Cannot verify' is a
%      result you want to get" merge into one slide -- "cannot verify" is
%      already one of the claim-tracing table's three row statuses
%      (alongside verified/mismatch), so a whole separate slide for one
%      status was more than it needed.
% All five Task 5 slides now carry the "Task 5 --" prefix, matching the
% pattern already established in Act I's Task 1-3 and Act II's Task 4.
%
% Uses \good/\bad/\unknown for the first time in the deck (main.tex) --
% the claim-tracing table is exactly the three-way verified/mismatch/
% cannot-verify status those macros exist for.

\stagedivider{4}{Act III: Challenge}{Do you believe it?}

% Combined 2026-09-03 at Alex's request: the old setup slide (just a
% question list) and the old 3-column "merging reads as an attack" slide
% merge into one -- the 3-column framing named only arithmetic/method/
% transcription, narrower than Alex's real 5-question checklist below
% (which also covers reproducibility and literature-claim fidelity), so
% the closing point is generalized rather than re-drawn as a 3-box panel.
\begin{frame}{Task 5 --- audit a manuscript before it's submitted}
  \vfill
  \begin{center}
  \begin{itemize}\small\setlength\itemsep{0.4em}
    \item Do the methods described match what the code actually did?
    \item Are there arithmetic mistakes in any reported number?
    \item Can every reported number be traced back to a results artifact?
    \item Does rerunning the analysis reproduce it, on a different machine?
    \item Do the manuscript's literature claims match the papers themselves?
  \end{itemize}
  \end{center}
  \vspace{0.8em}
  \begin{center}
    \slidepoint{Ask these \textbf{separately} ---\\together, they read as an \textbf{attack}, not a method.}
  \end{center}
  \vfill
\end{frame}

% NEW 2026-09-03 at Alex's request (the other half of the original TODO:
% "walk through using Claude to answer the questions as part of an
% audit"). Deliberately mechanics, not a transcript -- Alex's own call,
% 2026-09-03: no invented "Claude said..." dialogue, since the deck's
% standard is real/git-verified material and a fabricated exchange would
% be the one exception to that.
\begin{frame}{Task 5 --- walk it through, one question at a time}
  \vfill
  \begin{center}
    Point Claude at the manuscript PDF and the analysis repo --- not a summary of either, the actual files.
  \end{center}
  \vspace{0.6em}
  \begin{center}
  \begin{itemize}\small\setlength\itemsep{0.4em}
    \item Ask \textit{one} question from the list --- a merged prompt reads as an attack, same as before
    \item Traceability: grep the repo for the number, report the file --- or say it can't be found
    \item Method: diff the manuscript's description against what the code actually does
    \item Arithmetic: recompute from the underlying numbers, don't just re-read them
  \end{itemize}
  \end{center}
  \vspace{0.6em}
  \begin{center}
    \slidepoint{The audit is only as good as the \textbf{files} you hand it.}
  \end{center}
  \vfill
\end{frame}

% TODO(image_plan.md Act III #2): numbers are illustrative/synthetic
% (from outline.md's original example), never sourced from any real
% unpublished audit -- keep it that way on any future edit of this table.
\begin{frame}{Task 5 --- trace every number back to the file that produced it}
  \vfill
  \begin{center}
  \begin{tabular}{@{}p{5.4cm}p{2.6cm}p{2.6cm}l@{}}
    \toprule
    \textbf{Manuscript claim} & \textbf{Location} & \textbf{Source} & \textbf{Status} \\
    \midrule
    Costs decrease by 17\% & Results \S4.2 & \texttt{cost.csv} & \good{Verified} \\
    Reaction proceeds in 3 steps & Methods \S2.1 & \texttt{mechanism.py} & \bad{Mismatch (4)} \\
    Yield approximately doubles & Abstract & unknown & \unknown{Cannot verify} \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vspace{0.5em}
  \begin{center}
    Every claim gets a row before the reviewers make you build one.
  \end{center}
  \vspace{0.5em}
  \begin{center}
    \slidepoint{\unknown{Cannot verify} is a result you want ---\\a real \textbf{provenance gap}, not a failure of the audit.}
  \end{center}
  \vfill
\end{frame}

\begin{frame}{Task 5 --- a clean build hid a silent failure}
  \vfill
  \begin{columns}[t]
    \begin{column}{0.31\textwidth}
      \centering
      A manuscript's nomenclature table rendered empty.\\[0.4em]
      {\scriptsize Build exit code: 0.}
    \end{column}
    \begin{column}{0.31\textwidth}
      \centering
      A course's \texttt{.gitignore} silently dropped an artifact.\\[0.4em]
      {\scriptsize CI stayed green for 1.5 days.}
    \end{column}
    \begin{column}{0.31\textwidth}
      \centering
      A project's own \texttt{CLAUDE.md} warned about stale paths.\\[0.4em]
      {\scriptsize Directly above four stale paths.}
    \end{column}
  \end{columns}
  \vfill
  \begin{center}
    \slidepoint{A request to \textbf{enumerate} beats a request to \textbf{confirm}.}
  \end{center}
  \vfill
\end{frame}

\begin{frame}{Task 5 --- my status report was wrong about eleven items}
  \vfill
  \begin{center}
    \bad{\large "Everything is up to date."}
  \end{center}
  \vspace{0.4em}
  \begin{center}
    That was memory, not evidence. A script that actually checked found eleven items it was wrong about.
  \end{center}
  \vfill
  \begin{center}
    \slidepoint{Determine status from \textbf{evidence}, never \textbf{recollection}.}
  \end{center}
  \vfill
\end{frame}

% TODO(image_plan.md Act III capstone): capture a real screenshot of
% ndcbe.github.io/optimization (public course site) and, if available, a
% page of the converted coursepack showing the corrected sign. Both
% public -- Alex's own course. \placeholderimage below stands in until
% it's captured.
\begin{frame}{Task 6, part 1 --- the Act I argument, taken all the way}
  \vfill
  \begin{columns}[c]
    \begin{column}{0.34\textwidth}
      \centering
      \placeholderimage{4.0cm}{2.1cm}{screenshot: course site (ndcbe.github.io)}
    \end{column}
    \begin{column}{0.62\textwidth}
      \keyterm{Optimization for Decision Science} --- Alex's own course, already public.
    \end{column}
  \end{columns}
  \vspace{0.6em}
  \begin{columns}[t]
    \begin{column}{0.46\textwidth}
      \centering \textbf{Converted}\\[0.4em]
      Handwritten lecture notes $\to$ LaTeX, the coursepack.
    \end{column}
    \begin{column}{0.46\textwidth}
      \centering \textbf{Challenged}\\[0.4em]
      An adversarial verification pass catches a real, decade-old sign error.
    \end{column}
  \end{columns}
  \vfill
  \begin{center}
    The same \texttt{CLAUDE.md} pattern Task 2 explained generically --- now a mature, specific instance, watched end to end.
  \end{center}
  \vfill
\end{frame}

\begin{frame}{Task 6, part 2 --- the verifier needed verifying too}
  \vfill
  \begin{center}
    The verification pass ran three independent checks on the corrected sign.
  \end{center}
  \vspace{0.6em}
  \begin{center}
    \unknown{\large One of the three checks was itself wrong.}
  \end{center}
  \vspace{0.6em}
  \begin{center}
    Corrected before it shipped --- the same self-correction principle as Task 5's audits, now from a second domain.
  \end{center}
  \vfill
  \begin{center}
    \slidepoint{The checker needs \textbf{checking too}.}
  \end{center}
  \vfill
\end{frame}
