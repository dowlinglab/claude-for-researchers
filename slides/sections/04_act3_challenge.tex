% ========================= ACT III: CHALLENGE =========================
% storyboard.md Act III, revised 2026-09-03 around Task 5 and Task 6. Job:
% AI tells you that you're wrong, and you have to decide whether to
% believe it. Task 5 is manuscript audits (Type 2, generic per the naming
% convention -- unpublished work). Task 6, the capstone, names
% "Optimization for Decision Science" (Alex's own, already-public course)
% and watches one project across Understand -> Build -> Challenge.
%
% REVISED 2026-09-03, Alex reading the outline directly: "Does Task 5 need
% that many slides? We need a slide to set up Task 5." Two changes, net
% slide count unchanged:
%   1. A new setup slide (below) states the task before any technique
%      does -- every other task (1-4, in Act I/II) opens with an explicit
%      "Task N --" framing; Task 5 didn't, it jumped straight into a
%      method point.
%   2. "Trace every number back to the file" and "'Cannot verify' is a
%      result you want to get" merge into one slide -- "cannot verify" is
%      already one of the claim-tracing table's three row statuses
%      (alongside verified/mismatch), so a whole separate slide for one
%      status was more than it needed.
% All five Task 5 slides now carry the "Task 5 --" prefix, matching the
% pattern already established in Act I's Task 1-3 and Act II's Task 4.
%
% Uses \good/\bad/\unknown for the first time in the deck (main.tex) --
% the claim-tracing table is exactly the three-way verified/mismatch/
% cannot-verify status those macros exist for.

\stagedivider{4}{Act III: Challenge}{Do you believe it?}

% Combined 2026-09-03 at Alex's request: the old setup slide (just a
% question list) and the old 3-column "merging reads as an attack" slide
% merge into one -- the 3-column framing named only arithmetic/method/
% transcription, narrower than Alex's real 5-question checklist below
% (which also covers reproducibility and literature-claim fidelity), so
% the closing point is generalized rather than re-drawn as a 3-box panel.
\begin{frame}{Task 5 --- audit a manuscript before it's submitted}
  \vfill
  \begin{center}
  \begin{itemize}\small\setlength\itemsep{0.4em}
    \item Do the methods described match what the code actually did?
    \item Are there arithmetic mistakes in any reported number?
    \item Can every reported number be traced back to a results artifact?
    \item Does rerunning the analysis reproduce it, on a different machine?
    \item Do the manuscript's literature claims match the papers themselves?
  \end{itemize}
  \end{center}
  \vspace{0.8em}
  \begin{center}
    \slidepoint{Ask these \textbf{separately} ---\\together, they read as an \textbf{attack}, not a method.}
  \end{center}
  \vfill
\end{frame}

% NEW 2026-09-03 at Alex's request (the other half of the original TODO:
% "walk through using Claude to answer the questions as part of an
% audit"). Deliberately mechanics, not a transcript -- Alex's own call,
% 2026-09-03: no invented "Claude said..." dialogue, since the deck's
% standard is real/git-verified material and a fabricated exchange would
% be the one exception to that.
\begin{frame}{Task 5 --- walk it through, one question at a time}
  \ndlogonote{\scriptsize\texttt{resources/prompts/manuscript\_audit.md}}
  \vfill
  \begin{center}
    Point Claude at the manuscript source files (LaTeX in Overleaf synced via GitHub ;) and the analysis repo.
  \end{center}
  \vspace{0.6em}
  \begin{center}
  \begin{itemize}\small\setlength\itemsep{0.4em}
    \item Ask \textit{one} question from the list --- a merged prompt reads as an attack, same as before
    \item Traceability: grep the repo for the number, report the file --- or say it can't be found
    \item Method: diff the manuscript's description against what the code actually does
    \item Arithmetic: recompute from the underlying numbers, don't just re-read them
  \end{itemize}
  \end{center}
  \vspace{0.6em}
  \begin{center}
    \slidepoint{The audit is only as good as the \textbf{files} you hand it.}
  \end{center}
  \vspace{1.8cm plus 1fill}
\end{frame}

% TODO(image_plan.md Act III #2): numbers are illustrative/synthetic
% (from outline.md's original example), never sourced from any real
% unpublished audit -- keep it that way on any future edit of this table.
\begin{frame}{Task 5 --- trace every number back to the file that produced it}
  \ndlogonote{\scriptsize\texttt{resources/templates/results\_manifest.md}}
  \vfill
  \begin{center}
  \begin{tabular}{@{}p{5.4cm}p{2.6cm}p{2.6cm}l@{}}
    \toprule
    \textbf{Manuscript claim} & \textbf{Location} & \textbf{Source} & \textbf{Status} \\
    \midrule
    Costs decrease by 17\% & Results \S4.2 & \texttt{cost.csv} & \good{Verified} \\
    Reaction proceeds in 3 steps & Methods \S2.1 & \texttt{mechanism.py} & \bad{Mismatch (4)} \\
    Yield approximately doubles & Abstract & unknown & \unknown{Cannot verify} \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vspace{0.5em}
  \begin{center}
    Every claim gets a row before the reviewers make you build one.
  \end{center}
  \vspace{0.5em}
  \begin{center}
    \slidepoint{\unknown{Cannot verify} is a result you want ---\\a real \textbf{provenance gap}, not a failure of the audit.}
  \end{center}
  \vspace{1.8cm plus 1fill}
\end{frame}

% NEW 2026-09-04, from the same overnight audit wave: two independent real
% manuscript audits (different repos, different domains -- a technical
% force-field paper and a sustainability-policy paper) each independently
% turned up a citation-tracing failure of exactly this shape, generalized
% here with no repo/student names per the standing convention. A DOI
% checker's "propose, never write" habit (already resources/scripts/
% doi_checker/'s own design principle) is what caught the false positive;
% the citation-two-numbers-away case was a self-correction, found and
% named the next pass. Same claim-tracing operation as the previous
% slide's table, applied to a citation instead of a number.
\begin{frame}{Task 5 --- the same table works on a citation}
  \ndlogonote{\scriptsize\texttt{resources/scripts/doi\_checker/}}
  \vfill
  \begin{center}
    A DOI checker proposed a fix at a perfect title match --- to a completely unrelated document. Rejected by hand, before it looked exactly like the correct ones beside it.
  \end{center}
  \vspace{0.6em}
  \begin{center}
    A citation one reference number off looked identical, until someone actually opened both papers.
  \end{center}
  \vfill
  \begin{center}
    \slidepoint{A citation is a \textbf{claim} too --- \textbf{verified} or \textbf{wrong}, not close enough.}
  \end{center}
  \vspace{1.8cm plus 1fill}
\end{frame}

\begin{frame}{Task 5 --- a clean build hid a silent failure}
  \vfill
  \begin{columns}[t]
    \begin{column}{0.31\textwidth}
      \centering
      A manuscript's nomenclature table rendered empty.\\[0.4em]
      {\scriptsize Build exit code: 0.}
    \end{column}
    \begin{column}{0.31\textwidth}
      \centering
      A course's \texttt{.gitignore} silently dropped an artifact.\\[0.4em]
      {\scriptsize CI stayed green for 1.5 days.}
    \end{column}
    \begin{column}{0.31\textwidth}
      \centering
      A project's own \texttt{CLAUDE.md} warned about stale paths.\\[0.4em]
      {\scriptsize Directly above four stale paths.}
    \end{column}
  \end{columns}
  \vfill
  \begin{center}
    \slidepoint{A request to \textbf{enumerate} beats a request to \textbf{confirm}.}
  \end{center}
  \vfill
\end{frame}

\begin{frame}{Task 5 --- my status report was wrong about eleven items}
  \vfill
  \begin{center}
    \bad{\large "Everything is up to date."}
  \end{center}
  \vspace{0.4em}
  \begin{center}
    That was memory, not evidence. A script that actually checked found eleven items it was wrong about.
  \end{center}
  \vfill
  \begin{center}
    \slidepoint{Determine status from \textbf{evidence}, never \textbf{recollection}.}
  \end{center}
  \vfill
\end{frame}

% ======================================================================
% Task 6 (NEW 2026-09-04, REBUILT 2026-09-04 -- "inherit a project -> an
% auditable draft manuscript." Home for three practice files
% (technical_writing.md, writing_style_guide.md, scientific_figures_tables.md
% -- the last also anchored by Task 7) that had no live task in the talk.
% Generic (Type 1 naming convention).
%
% REBUILT at Alex's direction: the original build continued the Task 2
% membrane-transport project (data3_regression), but that project is still
% "figuring out the science" -- Task 2 territory, not Task 6's. This
% project has a real drafted manuscript, so it anchors Task 6 instead, with
% explicit callbacks to Task 2 (context/reproduction) and Task 3
% (literature review), per Alex's own framing: "starting the Task 2 [work]
% to understand the context/reproduce the existing results and then using
% Task 3 ideas to inform the literature review."
%
% Grounded in a real, authorized audit of
% `/Users/adowling/GitHub/DowlingLab/CDSE_hybrid_modeling_project` (202
% commits). ANONYMIZATION CAUTION (stronger than usual here): this repo's
% own git author, a data-directory name, a Python module name, and several
% docs name real individuals -- never screenshot, never speak the repo
% name or any path from it aloud, generic/paraphrased only, same as below.
%
%  - Slide 1 (Task 2 callback): a one-day inheritance (last student commit
%    2026-05-10, first Alex commit 2026-05-11, `b2db55d`) of a notebook-
%    and-data dump with no tests or package structure; the following three
%    weeks reproduced its own ~15 numbered experiments one at a time
%    (commits 2026-05-11--05-29) before anything was corrected. The actual
%    bug-found-by-regression-harness story from this repo (a real physics
%    bug in the *inherited* logic, found and fixed via A/B against a
%    third-party reference, reversing the paper's best-model conclusion --
%    commits `66e7574`..`4146244`, 2026-06-25/26) is deliberately NOT
%    retold here: it's the same shape as Task 4's bits_for_gaps centerpiece
%    and would read as repetitive back to back. Reserved for
%    scientific_computing_workflow.md \S4 instead (no worked example there
%    previously).
%  - Slide 2 (Task 3 callback): a full-library citation audit, 76 cited
%    papers checked directly against their own PDFs (not memory), found
%    six wrong -- including one citation with the source paper's finding
%    stated backwards (`cc38453`, 2026-07-06); DOI suffixes inferred
%    rather than read were flagged `% VERIFY` rather than guessed
%    (`693df12`). Secondary point: a later style pass restyled the
%    manuscript to the group's own established voice from an earlier
%    paper, once results were locked (`4bae3a7`, generalized -- no paper
%    named).
%  - Slide 3: the real "audit before anyone else's" moment -- a full
%    manuscript-vs-code self-audit, 25 discrepancies found, 22 fixed on
%    the spot, 3 explicitly left flagged for the author to decide
%    (`0b2e280`/`d5de7bd`, 2026-08-27, `docs/codex_audit_report.md`).
%    Same shape as Task 5's own audits, one task later, on your own draft.
\begin{frame}{Task 6 --- inherited a notebook, understood before it changed (Task 2)}
  \ndlogonote{\scriptsize\texttt{resources/practices/technical\_writing.md}}
  \vfill
  \begin{center}
  \begin{tikzpicture}[font=\small, node distance=1.2cm]
    \node[draw=NDBlue, rounded corners=2pt, minimum width=3.0cm, minimum height=1.1cm, align=center]
      (a) {Notebook + data\\\scriptsize no tests, no structure};
    \node[draw=NDBlue, rounded corners=2pt, minimum width=3.4cm, minimum height=1.1cm, align=center, right=of a]
      (b) {Understood, reproduced\\\scriptsize (Task 2)};
    \node[draw=NDGold, rounded corners=2pt, minimum width=3.0cm, minimum height=1.1cm, align=center, right=of b, fill=CodeBg]
      (c) {Draft\\\scriptsize manuscript};
    \draw[-{Stealth[length=6pt]}, thick] (a) -- (b);
    \draw[-{Stealth[length=6pt]}, thick] (b) -- (c);
  \end{tikzpicture}
  \end{center}
  \vspace{0.6em}
  \begin{center}
    A one-day handoff: the last commit from the original notebook, the first from the person who'd write the paper.
  \end{center}
  \vspace{0.4em}
  \begin{center}
    The next three weeks reproduced its fifteen numbered experiments, one at a time, before anything was corrected.
  \end{center}
  \vfill
\end{frame}

\begin{frame}{Task 6 --- verify the literature the same way you verify the code (Task 3)}
  \ndlogonote{\scriptsize\texttt{resources/practices/literature\_review.md}\\\texttt{writing\_style\_guide.md}}
  \vfill
  \begin{center}
    Every cited paper was checked against its own PDF, not memory: seventy-six papers, six citations wrong --- one with the finding stated backwards.
  \end{center}
  \vspace{0.6em}
  \begin{center}
    Once the numbers were locked, the manuscript was restyled to match the group's own established voice from an earlier paper.
  \end{center}
  \vfill
  \begin{center}
    \slidepoint{The same discipline as \textbf{Task 3} --- a citation is \textbf{checked}, not \textbf{assumed}.}
  \end{center}
  \vspace{1.8cm plus 1fill}
\end{frame}

\begin{frame}{Task 6 --- the manuscript audits itself, before anyone else does}
  \ndlogonote{\scriptsize\texttt{resources/practices/manuscript\_audit.md}\\\texttt{resources/templates/results\_manifest.md}}
  \vfill
  \begin{center}
    A full self-audit checked the manuscript against its own code and data: twenty-five discrepancies found, twenty-two fixed on the spot.
  \end{center}
  \vspace{0.6em}
  \begin{center}
    Three were left flagged, not silently resolved --- decisions only the author could make.
  \end{center}
  \vfill
  \begin{center}
    \slidepoint{The loop \textbf{closes} on itself --- the project audits its own paper.}
  \end{center}
  \vspace{1.8cm plus 1fill}
\end{frame}

% ======================================================================
% Task 7 (renumbered from Task 6, 2026-09-04) -- "Optimization for Decision
% Science," rebuilt from scratch. Alex's own call, reviewing the resources
% audit: replace the old two generic slides (a converted/challenged summary
% and a "verifier needed verifying too" beat) with real material from a
% freshly authorized audit of the course's public/private repo pair --
% local clones at /Users/adowling/GitHub/DowlingLab/Teaching/{optimization,
% optimization-private}. "Shrink" per Alex's own framing means tighter
% framing per slide, not less real content: the generic writing/style/
% figures teaching point moved to Task 6 above, freeing this task to be
% the dense, specific war story instead of competing for the same space.
%
% Headline, git-verified: optimization-private has 1,108 commits total;
% 1,085 of them (98%) landed 2026-08-17 -- 2026-09-02, a ~2.5-week
% pre-semester sprint (the repo itself dates to 2023-01-13, with only 23
% sparse commits before the sprint -- a burst in early 2023, then near-
% silence through 2024, the same "years of patches, then a burst" shape as
% the Prologue's grad-visit-scheduler hook). optimization (public) adds
% another 368 commits in the same window. The private repo's specifics
% (assignments, solutions, exam content) stay unnamed and unquoted --
% only Alex's own process is described, matching the deck's existing
% convention for private material.
%
% Slide-by-slide sourcing:
%  1. JupyterBook migration + the sprint's scale -- 67e200c/c4e9622/feb00d2
%     (optimization, 2026-08-17); commit-count arithmetic above.
%  2. Scan-retirement thread -- fa05519, c25c592, e4c291d/34bcb01/288e683,
%     8f9a080 (optimization); dowling.mplstyle already ships in
%     resources/scripts/figure_style/.
%  3. INK_TRANSCRIPTION_2026-08-22.md (optimization-private/claude/
%     lecture-feedback/) -- the 45-page annotated-PDF transcription.
%  4. OVERNIGHT_REPORT_2026-08-22.md (same directory) -- the false-
%     Digital-Discovery-citation catch (cross-checked against Alex's own
%     MyPublications.bib), the refused Simon-1960 page number, the
%     self-corrected citation count (79 claimed, 75 real).
%  5. optimization-private/CLAUDE.md, "Record COMPLETION where the reader
%     will look -- four instances in one day" (2026-08-22) -- close
%     parallel to Task 5's own "clean build hid a silent failure" slide;
%     kept as a separate, sharper instance rather than merged, since it
%     names the operating rule ("close the flag where the flag lives")
%     that slide only illustrates.
%  6. CODEX_HANDOFF.md (d9f6e35, 2026-08-26) -- confirmed directly by Alex
%     ("In the past ~2 weeks, I started using Codex with GPT-5.6 models");
%     the brief for the prose-trim task cites two real diffs of Alex's own
%     hand-edits (c9ec60e, adca23d) and \authored{} span counts (L3: 40,
%     L4: 63), not a vague "make it shorter" instruction.
\begin{frame}[fragile]{Task 7 --- a week before the semester, at scale}
  \vfill
  \begin{columns}[c]
    \begin{column}{0.34\textwidth}
      \centering
      \placeholderimage{4.0cm}{2.1cm}{screenshot: course site (ndcbe.github.io)}
    \end{column}
    \begin{column}{0.62\textwidth}
      \keyterm{Optimization for Decision Science} --- Alex's own course, already public, small manual pushes since 2023.
    \end{column}
  \end{columns}
  \vspace{0.6em}
  \begin{center}
    One pre-semester sprint, across the public course repo and its private companion: \textbf{$\sim$1{,}450 commits in 2.5 weeks} --- 98\% of the private repo's \textit{entire} history.
  \end{center}
  \vfill
  \begin{center}
    \slidepoint{The same shape as the Prologue's hook --- \textbf{years of patches}, then a \textbf{burst}.}
  \end{center}
  \vfill
\end{frame}

\begin{frame}{Task 7 --- retire the scan, own the figure}
  \ndlogonote{\scriptsize\texttt{resources/practices/scientific\_figures\_tables.md}\\\texttt{resources/scripts/figure\_style/}}
  \vfill
  \begin{center}
    JupyterBook 1 $\to$ 2, migrated and republished in one day.
  \end{center}
  \vspace{0.6em}
  \begin{center}
    Then, running through the whole sprint: eight orphaned textbook scans deleted, three textbook figures redrawn in TikZ, five book-table scans replaced with native Markdown.
  \end{center}
  \vfill
  \begin{center}
    \slidepoint{A figure the repo can \textbf{regenerate} beats one it can only \textbf{redisplay}.}
  \end{center}
  \vspace{1.8cm plus 1fill}
\end{frame}

\begin{frame}{Task 7 --- ink to LaTeX, verified page by page}
  \vfill
  \begin{center}
    A 45-page annotated instructor PDF, rendered and read one page at a time --- not skimmed from the typed notes alone.
  \end{center}
  \vspace{0.6em}
  \begin{center}
    Caught: an unnumbered page (a hand sketch) that had thrown off every page count after it, and a genuine contradiction between two of Alex's own annotations --- resolved to the one reading that made both true.
  \end{center}
  \vfill
  \begin{center}
    \slidepoint{The \textbf{ink}, not just the \textbf{typing that describes it}.}
  \end{center}
  \vfill
\end{frame}

\begin{frame}{Task 7 --- an overnight report, self-auditing as it went}
  \vfill
  \begin{center}
  \begin{itemize}\small\setlength\itemsep{0.5em}
    \item \bad{A false citation}, caught: a paper cited as published --- Alex's own \texttt{MyPublications.bib} says \textit{under review}. Corrected.
    \item \unknown{No page number invented} for a 1960 book not on hand --- a citation with a known gap beats a plausible guess.
    \item \bad{Its own citation count was wrong} --- 79 reported, 75 real. Recounted and corrected before it shipped.
  \end{itemize}
  \end{center}
  \vspace{0.6em}
  \begin{center}
    \slidepoint{Written \textbf{as} the work happened --- and it audited \textbf{itself}.}
  \end{center}
  \vfill
\end{frame}

\begin{frame}{Task 7 --- close the flag where the flag lives}
  \ndlogonote{\scriptsize\texttt{resources/practices/working\_with\_ai\_agents.md} \S9}
  \vfill
  \begin{center}
    One day, four separate fixes recorded somewhere nobody would re-read: a follow-up buried in a table, a stale source comment, a caption fix noted only in a commit message, an invisible tag.
  \end{center}
  \vspace{0.6em}
  \begin{center}
    Each cost real rework --- one left open for three days, in the file that loads every session; another redone from scratch a day later.
  \end{center}
  \vfill
  \begin{center}
    \slidepoint{A commit message is a \textbf{record}, not a \textbf{notification}.}
  \end{center}
  \vspace{1.8cm plus 1fill}
\end{frame}

\begin{frame}{Task 7 --- pick the tool for the task, grounded in evidence}
  \vfill
  \begin{center}
    Partway through, some of this work moved to Codex with GPT-5.6 --- not a wholesale switch, a choice per task.
  \end{center}
  \vspace{0.6em}
  \begin{center}
    The brief for a prose trim wasn't ``make it shorter'' --- it was two real diffs of Alex's own hand-edits, and a count of exactly how much of each lecture was new exposition, not transcribed from an old scan.
  \end{center}
  \vfill
  \begin{center}
    \slidepoint{The \textbf{brief} came from evidence --- the \textbf{tool} came second.}
  \end{center}
  \vfill
\end{frame}
